{#
/**
 * @file
 *
 * Available variables:
 * - raw_badge: Array of data about the badge straight from the Bitbucket API.
 * - image: Badge image.
 * - name: The name of the badge.
 *
 * @ingroup themeable
 */
#}
{%- set attributes = attributes.addClass('bitbucket-badge') %}
<figure{{ attributes }}>
  {{ image }}
  <figcaption>{{ name }}</figcaption>
</figure>
